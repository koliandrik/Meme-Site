<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width-device-width, initial-scale-1.0">
    <link rel="stylesheet"href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel-"stylesheet" href="/css/styles.css">
    <title>Meme Poster</title>
</head>

<body class="grey lighten-2">
    <header>
        <h1 class="brand-logo center">Post Your Meme!</h1>
        <div class="container">
            <form class="container row center" id="upload-url-form">
                <input type="text" id="image-url" placeholder="Enter Image URL" class="validate center white"/>
                <button class="btn waves-effect waves-light center" type="submit">Enter</button>
            </form>
        </div>
        <nav>
            <ul class="right">
                {{#if logged_in}}
                <li><a id="logout" href="#">Sign Out</a></li>
                {{else}}
                <li><a href="/signin">Sign In</a></li>
                <li><a href="/signup">Sign Up</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>

    {{{body}}}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}

</body>

</html>