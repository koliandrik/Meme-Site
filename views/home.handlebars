{{!== views/home.handlebars ==}}
<section id="feed">
  {{#each posts}}
    <div class="post">
      <img src="{{this.url}}" alt="Meme" />
      <p>{{this.caption}}</p>
    </div>
  {{/each}}
</section>

<script src="/js/home.js"></script>